# M03 物理文件资产与去重

## 模块目标

管理本地物理文件资产，并基于 SHA256 做跨任务去重。

## 当前 Step 0 范围

仅保留说明文件、注册表条目和 `backend/uploads` 目录。

## 对外接口

暂无。

## 非目标

不实现文件上传、SHA256 计算、文件入库或重复文件判断。

## 扩展预留

物理文件资产后续应与任务文件引用解耦，并预留 `sha256`、`storage_path`、`mime_type`、`size_bytes`、`metadata`。
