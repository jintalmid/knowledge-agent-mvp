# M04 任务文件引用

## 模块目标

维护任务空间内的文件引用，使一个物理文件资产可被多个任务复用。

## 当前 Step 0 范围

仅保留说明文件和注册表条目。

## 对外接口

暂无。

## 非目标

不实现任务文件绑定、解绑、重命名或引用查询。

## 扩展预留

引用模型后续应预留 `task_id`、`file_asset_id`、`display_name`、`source_role`、`parse_status`。
