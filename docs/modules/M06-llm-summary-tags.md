# M06 LLM 摘要与标签

## 模块目标

通过统一 LLM Service 为文件生成摘要、标签和结构化描述。

## 当前 Step 0 范围

仅保留说明文件、注册表条目和 LLM 环境变量样例。

## 对外接口

暂无。

## 非目标

不实现 LLM Service、摘要生成、标签生成或 prompt 模板。

## 扩展预留

所有 LLM 调用必须经统一服务，并由 M13 记录调用日志。没有模型配置时必须明确报错。
