# M07 Chunk / RAG 配置预留

## 模块目标

为后续 Chunk、Embedding、向量索引和 RAG 参数保留配置位置。

## 当前 Step 0 范围

仅保留说明文件和注册表条目。

## 对外接口

暂无。

## 非目标

不实现 Chunk、Embedding、向量数据库、召回或重排。

## 扩展预留

后续配置可包含 `chunk_size`、`chunk_overlap`、`embedding_model`、`retrieval_top_k`、`rerank_enabled`。
